export EDITOR="nvim"
export TERMINAL="st"
export MOZ_ENABLE_WAYLAND=1
eval "$(ssh-agent -s)"

# MPD daemon start (if no other user instance exists)
[ ! -s ~/.config/mpd/pid ] && mpd --no-config

# MPDScribble daemon start
[[ -z $(pgrep -xU $UID mpdscribble) ]] && mpdscribble &
